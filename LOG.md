# Reverse Engineering and Build Log

## 2026-07-06

- Confirmed workspace starts empty in the project checkout.
- Confirmed the local APK artifact was available for reverse engineering; APK files remain gitignored and are not part of the repo.
- Confirmed local tools available: `apktool`, `aapt`, `apksigner`, `unzip`, `node`, `npm`, `bun`, `python`, `java`.
- Decoded APK resources and smali with `apktool` into `apktool_out/`.
- Attempted `jadx`; system default Java is too old for installed jadx (`class file version 55` requires Java 11+, current runtime supports up to Java 8).
- Re-ran `jadx` with Java 17 and decompiled sources to `jadx_out/` (jadx reported 6 decompile errors but produced readable Java/Kotlin output).
- Found Android package `com.jellyworkz.mubert`, app version `4.2.2`, and network permission in `AndroidManifest.xml`.
- Found Retrofit provider base URL in `jadx_out/sources/defpackage/b0.java`: `https://api-app.mubert.com/`.
- Found API endpoints: `v2/Auth`, `v2/CheckAuth`, `v2/AppGetPages`, and stream-state endpoints under `com.jellyworkz.mubert.source.remote`.
- Found base request params from `BaseParams`: `os`, `application`, `version`, `language`, `sandbox`.
- Found app user-agent interceptor `pk3`: `Mubert/3.0 (...; <lang>) Android/<version>`.
- Found `MatInterceptor` stores the `Set-Cookie: mat=...` token from API responses.
- Found playback stream proxy requirement in the app architecture: stream URLs are fetched with the Mubert cookie (`mat`) and should be proxied server-side for browser playback/CORS.
- Auth confirmation attempts:
  - `https://mubert.com/v2/Auth` returned HTTP 404, confirming the public site is not the API base.
  - `https://api.mubert.com/v2/Auth` returned `{ "method":"Auth", "status":0, "error":{"code":250,"text":"This version is not supported, please update your app"}, "api_ver":"api_2.1.0" }`.
  - `https://api-app.mubert.com/v2/Auth` with the expected app payload shape returned an access-denied response until the required app-start session step was discovered.
  - The advisory-suggested `app:"Mubert"` field returned `{ "method":"Auth", "status":0, "error":{"code":102,"text":"Wrong request: 'application' not empty needed"}, "api_ver":"api-app_2.0" }`, confirming the server expects `application`, not `app`.
- Created a Node backend in `server.js` with `/api/login`, `/api/status`, `/api/channels`, and `/api/stream/:id`.
- Created a Vite-compatible frontend in `public/` with an app-inspired dark channel grid, login card, now-playing panel, and HTML audio player.
- Added `vite.config.js` to proxy `/api` to the backend in development; backend remains the CORS/cookie/audio proxy layer.
- Investigated app-local session behavior during debugging without committing tokens, account data, or device artifacts.
- Confirmed the app API requires a live `mat` session cookie for catalogue requests.
- Backend defaults to port `3100` to avoid the existing `:3000` conflict; Vite runs on `127.0.0.1:5173` and proxies `/api` to `127.0.0.1:3100`.
- Earlier testing used an environment-supplied session token outside the repo; the source tree does not include that token.
- Verified `/api/channels` returns the flattened playable channel list (first channel observed: `Elia`, stream `69631`, `https://stream.mubert.com/b2c/v2?sid=69631`).
- Verified stream proxy: `curl --max-time 5 -s -r 0-1000 http://127.0.0.1:3100/api/stream/69631 -o /dev/null -w '%{http_code} %{content_type}'` produced `200 audio/mpeg` and local byte captures contained audio data.
- Added an abort/cancel path in `proxyStream` so browser channel switches do not leak upstream stream connections; verified backend log no longer shows `AbortError` crashes after stream abort tests.
- Verified browser UI opens at `http://127.0.0.1:5173/`, loads the channel grid, and selecting the first card sets the player source to `/api/stream/69631` with now-playing text `Elia` / `#1 · by elia`.
- Follow-up auth fix: the missing step was `v2/AppStart`. Calling `POST https://api-app.mubert.com/v2/AppStart` with `{method:"AppStart", params:[], os:"Android", application:"Mubert", version:"4.2.2", language:"en-US", sandbox:false}` returns `Set-Cookie` session cookies; using that fresh cookie on `v2/Auth` makes valid app credentials succeed. The current nginx/static frontend runs this AppStart-then-Auth sequence in-browser via the relay, so it no longer depends on any externally supplied token.
- Verified login through the relay with app credentials and confirmed the channel catalogue loads without any committed session secret.
- Inspected Android layout XMLs for the UI revision: `email_fragment.xml`, `login_password_fragment.xml`, `main_fragment.xml`, and `play_layout.xml`. The browser app is now split into a login-first two-step flow (Email then Password) and a desktop-friendly player view inspired by the tablet layouts: top scrollable category tabs, vertical channel list with circular icons, and central artwork/player region.
- Verified revised UI at `http://127.0.0.1:5173/`: first view is the email login step; after entering valid credentials, the player loads with tabs (`artists`, `sleep`, `calm`, `focus`, ...), 41 visible rows for the active `artists` tab, and the player source `/api/stream/69631`.
- Re-verified stream proxy after the auth/UI revision: `curl --max-time 5 -s -r 0-1000 http://127.0.0.1:3100/api/stream/69631 -o /dev/null -w '%{http_code} %{content_type}'` returns `200 audio/mpeg`.
- Follow-up responsive/web-player UI update: removed non-functional fake like/dislike/shuffle/repeat controls and kept only features backed by available data/API (channel tabs, channel search/filter, channel list, artwork, track metadata, refresh, and audio playback). Reworked CSS away from fixed viewport assumptions: desktop keeps a two-pane library/player layout; narrower widths stack library and player without horizontal overflow. Verified at `1280x800` (`scrollWidth == viewport width`, 41 of 101 channels shown, no fake controls) and `820x900` (single-column responsive layout, no horizontal overflow).
- UI cleanup after review: removed the nonfunctional three-dot settings buttons from login/player because they did not do anything useful. Removed the top list fade overlay and active-row background fill that made the first channel look darkened; active state is now indicated by the circular artwork ring only. Verified no settings buttons remain, `.fade-top` is hidden, first row background is transparent, and the active tab still shows 41 of 101 channels.
- Feature/safety follow-up: unauthenticated direct stream access was tested with `https://stream.mubert.com/b2c/v2?sid=69631` and returns HTTP `403` with zero bytes, so auth/session cookie is required for music playback.
- Replaced the Node backend with a dumb nginx Docker relay. Removed `server.js` and `vite.config.js`; the current architecture is `docker-compose.yml` + `nginx/default.conf` serving `public/` and proxying same-origin paths.
- Relay routes: `/mubert-api/` proxies to `https://api-app.mubert.com/` with the Android `User-Agent`; `/mubert-stream/` proxies to `https://stream.mubert.com/` with browser cookies forwarded upstream. nginx rewrites Mubert cookies from `Domain=mubert.com; Secure` to the browser-visible relay host with non-secure `SameSite=Lax`, so it works on `http://127.0.0.1:8080` and should also work on whatever LAN host/IP the browser uses.
- Frontend now owns `AppStart`, `Auth`, `AppGetPages`, channel flattening, search/filter, selection, and playback URL construction. The relay only serves static files, rewrites cookies, and forwards API/audio bytes.
- Clarified channel metadata: the former stream count is now shown only as `N variants` when a unit exposes multiple alternate streams; it does not perform an action. Added a small note for light/heavy intensity support: the app API has `v2/AppLightMode` (`light_mode: "on"|"off"`) and channel `lightMode` flags, but this frontend does not wire intensity controls yet.
- Algorithm feedback investigation: Android exposes `v2/AppSetRate` with params `{like, dislike, time}` via `LikeState`/`LikeParam`; the current web app does not implement plus/minus because we have not verified its runtime semantics and removed fake controls rather than shipping misleading UI.
- Release safety: current source is safer for GitHub after removing hardcoded credentials and deleting the Node session backend. Do not commit `.env`, APK/decompile outputs, logs with extracted tokens, account data, or browser cookie exports.
- Verified nginx relay end-to-end in the browser at `http://127.0.0.1:8080`: login flow succeeds, browser stores `mat` and `mat_id` cookies for `127.0.0.1`, `AppGetPages` loads 101 channels, the first tab shows 41 rows, selecting a channel sets audio to `/mubert-stream/b2c/v2?sid=69631`, the network response is `200 audio/mpeg`, `<audio>` reaches `readyState: 3`, `currentTime` advances, `paused: false`, and there is no media error.
- Initialized a git repository with `git init`. Tightened `.gitignore` for local/generated artifacts: OS junk, cache/temp files, APK bundles, `node_modules/`, `.env*`, decompile outputs, and logs. Removed reverse-engineering helper scripts and stale Node/Vite/backend files so the repo tracks only the current nginx relay/static frontend implementation plus `LOG.md`, `README.md`, and `screenshot.png`.
- Rewrote `README.md` in the requested shape only: introduction, screenshot, Docker usage, and local editing instructions.
- Missing app features currently identified from decompiled API/layout findings: light/heavy intensity toggle via `v2/AppLightMode`; plus/minus algorithm feedback via `v2/AppSetRate`; favorites/add-remove units via `v2/AppAddUnit` and `v2/AppRemoveUnit`; stream mixing via `v2/AppMixStream`; loop state via `v2/AppSetLoopState`; explicit logout/session clearing via logout API; social auth, registration, password recovery, account removal/change password; push token/statistics/analytics; billing/receipt/promo-code flows; share/deep-link data via `v2/AppGetShared`; musician stream/search endpoints; and richer handling of multi-stream variants beyond showing `N variants` and using the first stream.
- Implemented the requested playback/app controls in the frontend: light/heavy intensity (`v2/AppLightMode`), plus/minus preference feedback with Android-style 2s debounce (`v2/AppSetRate`), save/unsave favorites (`v2/AppAddUnit` / `v2/AppRemoveUnit`) with client-side favorite state seeded from FAVES units, stream variant picker, mix request (`v2/AppMixStream`), loop toggle (`v2/AppSetLoopState` + HTML audio loop), and logout/session clearing (`v2/SignOut` + local cookie/localStorage cleanup).
- Verified control behavior in-browser through the nginx relay. Successful observed responses: `AppLightMode` -> `status:1`, `Light mode is on`; `AppSetRate` -> `status:1`, `Rate added`; `AppAddUnit` -> `status:1`, `Favorite Added`; `AppRemoveUnit` -> `status:1`, `Favorite Dropped`; `AppSetLoopState` -> `status:1`, `Loop state is ON`; logout returns to login and clears browser cookies. Variant picker switches from `sid=69631` to `sid=69685`. `AppMixStream` is wired with the raw `stream.url` confirmed by decompiled callsites, but Mubert currently returns `status:0`, code `106`, `Wrong params: 'params->stream' wrong link` for the tested streams, so the UI reports that failure instead of pretending it succeeded.
- Replaced the earlier convenient screenshot with a better representative player screenshot showing a selected channel, action controls, variant selector, audio player, and channel list.
- UX audit note: current feature pass exposed too many low-frequency/account actions in the primary playback row. Logout beside playback controls is especially poor hierarchy; it should move to a secondary account/menu area. Recommended grouping: primary transport/stream controls near audio (`variant`, `loop`, maybe `mix` only if proven useful), personalization controls as a separate compact row (`Save`, `Less`, `More`, `Light/Heavy`), and account/session controls away from the player chrome. The screenshot also shows cramped lower controls and too much tiny all-caps text around the track area.
- Applied the UX cleanup from the audit: removed the dead password recovery button because this bare-minimum web player expects accounts to be created/recovered elsewhere; moved refresh and logout into the header; split per-track feedback (`Less`, `More`) from channel controls (`Save`, intensity, mix, loop); moved variant and audio into a dedicated player panel; separated persistent mode text from transient API status text; deleted the old FAB/action-row CSS and rewrote the player CSS section cleanly instead of continuing the accumulated absolute-position patches.
- Fixed the Special/experimental variant bug. Root cause was duplicate `unit.unid` values across pages; using only `unid` as the frontend channel id caused selecting Special/experimental (`unid=1`) and then changing variants to resolve against a different page's `unid=1` channel. Channel ids are now composite `page.pid:unit.unid`, while API calls still pass the numeric `unitId`. Verified Special page stream counts: Halloween 1, Product Hunt 2, daily 1, experimental 24, deep 8, groove 7. The experimental picker now switches from `sid=199` to `sid=148` without changing channels. Mix is disabled as `Mix unavailable` because Android also sends raw `stream.url`, and the observed API response remains server-side `status:0`, code `106`, `Wrong params: 'params->stream' wrong link`.
- Release polish: added a restrained `Open Mubert / Web Player` brand block to fill the empty header space, removed email persistence from localStorage, scrubbed the build log of local paths/device-token debugging details, recaptured `screenshot.png`, and visually reviewed the final screenshot for header fit, control grouping, alignment, and overflow.
- Tightened the header spacing by replacing the tall `14svh` bottom-aligned header with a compact content-sized header, eliminating the empty band above the brand/tabs. Added login-screen explanatory copy to both email and password steps: users sign in with an existing Mubert account, and only email/password auth is supported in this web app.
- Added a self-contained Docker image (`Dockerfile`) that copies `public/` and `nginx/default.conf` into `nginx:1.27-alpine`, kept docker compose as a bind-mounted local editing setup, added a GHCR publishing workflow under `.github/workflows/docker.yml`, and expanded README instructions for Docker, GHCR image naming, the same-origin Mubert API/stream relay paths, and normal nginx deployment.
